#ifndef UTILS_H
#define UTILS_H

#include <string>

// Задание 2: поменять местами каждые два соседних слова
// inputFile -> output to console (and optionally save to output file)
void swapWordsInFileAndPrint(const std::string& filename);

#endif // UTILS_H